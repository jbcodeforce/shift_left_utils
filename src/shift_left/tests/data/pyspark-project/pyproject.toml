[project]
name = "pyspark-project"
version = "0.1.0"
description = "PySpark examples for Catalyst plan export and Flink SQL translation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=3.5.0",
]
