CREATE TABLE IF NOT EXISTS equipment_stage_json_stream (
    SERVICE STRING,
    HOST STRING,
    PORT_ID STRING,
    BANDWIDTH INT,
    SYSTEM_ID STRING,
    SYSTEM_TYPE STRING,
    OCTETS_RX DOUBLE,
    OCTETS_TX DOUBLE,
    UNICAST_RX DOUBLE,
    UNICAST_TX DOUBLE,
    OP_STATE STRING,
    DISCARDS_TX DOUBLE,
    DISCARDS_RX DOUBLE,
    MULTICAST_RX DOUBLE,
    MULTICAST_TX DOUBLE,
    COLLECTOR_HOST STRING,
    ERRORS_TX DOUBLE,
    ERRORS_RX DOUBLE,
    BROADCAST_TX DOUBLE,
    BROADCAST_RX DOUBLE,
    taskId STRING,
    ADMIN_STATE STRING,
    "CONNECTOR" STRING,
    UTILIZATION_RX DOUBLE,
    UTILIZATION_TX DOUBLE,
    NUNICAST_RX DOUBLE,
    NUNICAST_TX DOUBLE,
    TOTAL_PKTS_RX DOUBLE,
    TOTAL_PKTS_TX DOUBLE,
    DISCARDS_RX_PCT DOUBLE,
    DISCARDS_TX_PCT DOUBLE,
    ERRORS_RX_PCT DOUBLE,
    ERRORS_TX_PCT DOUBLE,
    COLLECTION_TS STRING,
    DATETIMEUTC STRING,
    "time" STRING
) DISTRIBUTED BY HASH(SERVICE) INTO 1 BUCKETS WITH (
    'value.format' = 'json-registry',
    'key.json-registry.schema-context' = '.flink-dev',
    'scan.startup.mode' = 'earliest-offset',
    'value.fields-include' = 'all',
    'kafka.retention.time' = '0',
    'kafka.producer.compression.type' = 'snappy',
    'scan.bounded.mode' = 'unbounded'
);