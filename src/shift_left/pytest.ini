[pytest]
# Project root must be on path (package is ./shift_left/, not under ./src/)
pythonpath = ., tests
markers =
    integration: tests under tests/it/ (optional cloud; see SHIFT_LEFT_RUN_CLOUD_IT)
